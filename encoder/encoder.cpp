#include <string>
#include <iostream>

/*int encoder(std::string &message_code, std::string &key, int message_length)
{
    for (i = 1; i <= message_length ; ++i)
        *encoded_message += message_code[i-1] * key[i-1];
    return 0;
}*/
//есть месседже_код = 2132132
//есть ключ =         4 11
